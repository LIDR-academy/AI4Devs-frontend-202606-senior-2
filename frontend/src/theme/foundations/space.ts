// Figma has no spacing tokens; scale measured from the board (column padding 24, card padding 16, gaps 8/12).
// Keys follow Chakra's numeric convention (value / 4) so `p={4}` reads as 16px.
export const space = {
  '1': '4px',
  '2': '8px',
  '3': '12px',
  '4': '16px',
  '6': '24px',
};
