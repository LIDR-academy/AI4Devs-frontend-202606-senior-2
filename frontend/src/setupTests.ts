// Extends Jest's `expect` with the DOM matchers used by React Testing Library,
// e.g. `expect(element).toBeInTheDocument()`.
// Loaded automatically by `react-scripts test`.
import '@testing-library/jest-dom';
