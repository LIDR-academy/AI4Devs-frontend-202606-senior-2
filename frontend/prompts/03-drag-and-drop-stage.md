/opsx:propose "Add drag-and-drop to the Position kanban view: dragging a candidate card into a different column calls PUT /candidates/:id (id = candidate.id) with body { applicationId: candidate.applicationId, currentInterviewStep: <target column's interviewStep id> }, moves the card to that column in local state on success, and on failure keeps/reverts the card to its original column and shows an inline error, without losing the card."
