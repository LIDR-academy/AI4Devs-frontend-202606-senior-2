/opsx:propose "Create the Position detail page at frontend/src/components (kanban view), rendered as the internal content only — assume the global header/nav and footer already wrap the page, don't add them. Header shows the position title (bold) and a back arrow/link to /positions. Fetch GET /position/:id/interviewflow to get positionName and interviewFlow.interviewSteps (columns, ordered by orderIndex), and GET /position/:id/candidates to get candidates (each has id, applicationId, fullName, currentInterviewStep — the step name as a string — and averageScore) placed as a card under the column whose name matches currentInterviewStep. Follow the design team's mockup exactly: light-gray board columns with the step name as a bold header; white cards with NO visible border, only a soft shadow (no border line around the card); candidate fullName bold at the top of the card; averageScore rendered as exactly as many filled green dots as the (rounded) score — e.g. score 3 renders exactly 3 dots, score 0 renders none — NOT a fixed-size rating bar (no padding to 5 slots, no empty/gray placeholder dots after the filled ones). Keep id and applicationId in state even though not displayed, they're needed for the drag-and-drop stage update in a later change. Mobile: columns stack vertically, full width. No drag-and-drop yet, static layout only. Before considering this done, take an actual screenshot of the rendered page and compare it side-by-side against the mockup image — don't rely on DOM/class inspection alone."
