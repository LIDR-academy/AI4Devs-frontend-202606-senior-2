/opsx:propose "Create the Position detail page at frontend/src/components (kanban view), rendered as the internal content only — assume the global header/nav and footer already wrap the page, don't add them. Header shows the position title and a back arrow/link to /positions. Fetch GET /position/:id/interviewflow to get positionName and interviewFlow.interviewSteps (columns, ordered by orderIndex), and GET /position/:id/candidates to get candidates (each has id, applicationId, fullName, currentInterviewStep — the step name as a string — and averageScore) placed as a card under the column whose name matches currentInterviewStep. Follow the design team's mockup: light-gray board columns with the step name as a bold header, white cards with a soft shadow inside each column, candidate fullName bold at the top of the card, and averageScore rendered as a row of filled dot/circle indicators (a rating-style display) rather than a plain number. Keep id and applicationId in state even though not displayed, they're needed for the drag-and-drop stage update in a later change. Mobile: columns stack vertically, full width. No drag-and-drop yet, static layout only."
